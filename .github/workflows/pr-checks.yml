name: Pre-Merge Code Quality Checks

on:
  pull_request:
    branches:
      - "**"

jobs:
  lint-and-analyze:
    name: 📊 Prettier & PMD Static Analysis
    runs-on: ubuntu-latest
    container:
      image: jayadeepkumar61/sfdx-cicd-latest:latest
      credentials:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_PASSWORD }}
      options: --user root

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # Configure Git Safe Directory
      - name: ✅ Configure Git Safe Directory
        run: git config --global --add safe.directory $GITHUB_WORKSPACE

      # Fetch git diff changes
      - name: 🔍 Detect changed files
        id: changes
        run: |
          git fetch origin main

          CHANGED_FILES=$(git diff --name-only origin/main...HEAD || true)

          # Save Prettier-relevant files
          echo "$CHANGED_FILES" | grep -E '\.cls$|\.html$|\.js$|\.json$' || true > changed_code_files.txt

          # Save Apex classes
          echo "$CHANGED_FILES" | grep 'force-app/main/default/classes/.*\.cls$' || true > changed_apex_classes.txt

          echo "=== DEBUGGING ==="
          echo "Changed Code Files:"
          cat changed_code_files.txt || true
          echo "Changed Apex Classes:"
          cat changed_apex_classes.txt || true
          echo "=== DEBUGGING END ==="

      # Run Prettier on changed files
      - name: 🎨 Run Prettier Check on Changed Files
        id: prettier
        continue-on-error: true
        run: |
          if [ -z "$CHANGED_CODE_FILES" ]; then
            echo "⚡ No relevant files for Prettier."
            exit 0
          fi
          echo "🔍 Running Prettier on:"
          echo "$CHANGED_CODE_FILES"
          prettier --plugin=prettier-plugin-apex --check $CHANGED_CODE_FILES

      # Run PMD on changed Apex classes
      - name: 🧠 Run PMD Static Code Analysis on Changed Apex Classes
        id: pmd
        continue-on-error: true
        run: |
          if [ -z "$CHANGED_APEX_CLASSES" ]; then
            echo "⚡ No Apex classes changed. Skipping PMD."
            exit 0
          fi
          echo "🔍 Running PMD on:"
          echo "$CHANGED_APEX_CLASSES"
          pmd pmd -d force-app/main/default/classes -R rulesets/apex/quickstart.xml -f text

      - name: ❌ Fail if Prettier or PMD failed
        run: |
          echo "🧪 Checking previous step results..."
          if [ "${{ steps.prettier.outcome }}" == "failure" ] || [ "${{ steps.pmd.outcome }}" == "failure" ]; then
            echo "❌ One or more checks failed."
            exit 1
          else
            echo "✅ All checks passed!"
          fi
